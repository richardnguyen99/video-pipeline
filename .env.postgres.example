# Copy to `.env.postgres` (do not commit).
#   cp .env.postgres.example .env.postgres

POSTGRES_USER=video_pipeline
POSTGRES_PASSWORD=change-me-strong
POSTGRES_DB=video_pipeline
POSTGRES_PORT=5432

# Optional: bind-mount on external drive instead of Docker named volume
# POSTGRES_DATA_DIR=
