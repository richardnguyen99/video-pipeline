//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: true,
  bracketSpacing: true,
  singleQuote: false,
  printWidth: 120,
  trailingComma: "all",
};

export default config;
