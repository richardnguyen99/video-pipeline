/** Avatar size (size-9). */
export const AVATAR = 36;
/** Gap between avatar and content. */
export const GAP = 10;
/** Content padding-left = avatar + gap. */
export const PAD = AVATAR + GAP;
/** Trail x relative to the padded content box (centers under avatar). */
export const TRAIL_OFFSET = -(GAP + AVATAR / 2);
/** Absolute distance from trail to content left edge. */
export const TRAIL_INSET = GAP + AVATAR / 2;
/** Height of the branch curve region. */
export const BRANCH_H = 18;
/** Corner radius of the branch curve. */
export const CURVE_R = 10;
